Houses table
CREATE TABLE houses (
  house_id int PRIMARY KEY, 
  name text);

Photos table
CREATE TABLE photos (
  img_url text,
  img_order int,
  house_id int,
  PRIMARY KEY(house_id, img_order)
);
